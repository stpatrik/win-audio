{
    "name": "win-audio",
  "version": "1.1.0",
  "description": "Windows audio volume control (fork, without listener)",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "rebuild": "node-gyp rebuild"
  },
  "gypfile": true,
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "node-gyp-build": "^4.8.4"
  },
  "engines": {
    "node": ">=18"
  }
}
